# backend service base url, test environment (本地 FastAPI；dev 模式经 /proxy-default 代理)
VITE_SERVICE_BASE_URL=http://localhost:8000/api/v1

# other backend service base url, test environment
VITE_OTHER_SERVICE_BASE_URL= `{
  "demo": "http://localhost:8000"
}`
