# backend service base url, prod environment
# 生产部署必须改为真实后端地址（含 /api/v1），例如：
#   VITE_SERVICE_BASE_URL=https://api.example.com/api/v1
# 若前端与后端同域由 nginx 反向代理 /api 路径，可留空走相对路径。
# 严禁保留 Apifox mock 地址：否则生产构建的所有接口都会请求假数据。
VITE_SERVICE_BASE_URL=
